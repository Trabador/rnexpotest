export enum MainRoutes {
  Home = "Home",
  Details = "Details",
}

export enum LoginRoutes {
  Login = "Login",
}

export enum RootRoutes {
  Splash = "Splash",
  Login = "LoginRoot",
  Main = "MainRoot",
}
